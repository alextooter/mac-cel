language: objective-c # Not really, but we need an OSX build :-)
script: "
    CFLAGS=-m32 make && strip ./mac-cel && ./mac-cel && ./mac-cel -t && base64 < ./mac-cel;
    CFLAGS=-m64 make && strip ./mac-cel && ./mac-cel && ./mac-cel -t && base64 < ./mac-cel;
"
